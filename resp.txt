SELECT 
    snapshot_date,
    COUNT(DISTINCT account_id) AS total_accounts
FROM 
    account_snapshots
GROUP BY 
    snapshot_date
HAVING 
    COUNT(DISTINCT account_id) <= 100
ORDER BY 
    snapshot_date DESC;
