Field	Type	Group	Definition
goal_assignment_key	INT PK	Keys	Surrogate primary key. Unique identifier for each goal assignment row. Generated by the ETL on load.
org_key	INT FK	Keys	Foreign key to dim_org. Identifies who the goal is assigned to — an individual RM, a branch, or a team. Determines the org_type_cd of the goal owner.
goal_key	INT FK	Keys	Foreign key to dim_goal. Identifies which metric this goal is tracking — originations, balance, new accounts, etc. Also drives ETL routing via source_table_cd on dim_goal.
fiscal_year	INT	Goal period	The fiscal year this goal applies to. Example: 2025. Used to scope queries to the correct annual plan and to partition goal history across years in the same table.
goal_version_cd	VARCHAR(20)	Goal period	Identifies which version of the annual plan this goal came from. Example: FY2025_BUDGET. Allows multiple versions to coexist if goals are ever revised. Must match goal_version_cd on fact_goals_ytd_snap for the ETL join to work correctly.
annual_goal_amount	DECIMAL(18,2)	Goal amounts	The original full-year target as set by Finance or management at the start of the year. This is the single source of truth for the goal. Never changes once the year begins. Stored here once and prorated daily into fact_goals_ytd_snap by the ETL.
total_biz_days	INT	Goal amounts	Total number of business days in the fiscal year. Typically 261. Used as the denominator when prorating the annual goal to a daily amount. Sourced from dim_date at load time. Example: daily_goal = annual_goal_amount / total_biz_days.
goal_level_cd	VARCHAR(20)	Goal level	Indicates the organisational level this goal is set at. Values: RM, BRANCH, TEAM. Must align with the org_type_cd of the org_key row in dim_org. Used by the ETL and reports to separate RM-level goals from branch and team goals and avoid double counting.
set_by	VARCHAR(100)	Approval and ownership	Name or identifier of the person who set this goal. Typically a branch manager, team manager, or Finance representative. Provides an audit trail for who is accountable for each goal amount.
approved_by	VARCHAR(100)	Approval and ownership	Name or identifier of the person who formally approved this goal. May be the same as set_by for lower-level goals or a senior manager for larger goals. NULL if no formal approval process exists for this goal.
approved_date	DATE	Approval and ownership	The date this goal was formally approved and locked. Once approved_date is populated the annual_goal_amount should be treated as immutable. NULL if the goal has not yet been formally approved.
goal_set_date	DATE	Approval and ownership	The date this goal was initially entered into the system. May differ from approved_date if goals go through a review cycle before being locked. Used for auditing and for tracking how early in the year goals were finalised.
is_active_flag	CHAR(1)	Status	Y if this goal assignment is currently active and should be included in scorecard calculations. N to deactivate a goal without deleting it — for example if an RM leaves mid-year and their goal is retired. The ETL filters to is_active_flag = Y when loading fact_goals_ytd_snap.
load_date	DATE	Audit	The date this row was written by the ETL or goal loading process. Used for pipeline auditing and troubleshooting. Should match goal_set_date for manually entered goals.
